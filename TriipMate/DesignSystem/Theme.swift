//
//  Theme.swift
//  TriipMate
//
//  Replace these with the brand identity colors once Figma is locked.
//

import SwiftUI

enum TMTheme {
    static let accent  = Color.orange      // TODO: swap for brand orange
    static let surface = Color(.secondarySystemBackground)
    static let onSurface = Color.primary
}
